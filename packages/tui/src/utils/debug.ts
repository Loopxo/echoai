export function logForDebugging(_message: string, _options?: { level?: "verbose" | "debug" | "info" | "warn" | "error" }): void {
    // Vendored terminal engine debug sink. Keep silent by default in EchoAI.
}
