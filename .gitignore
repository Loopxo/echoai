# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
dist/
build/
*.tsbuildinfo
*.vsix

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock
evals/runs/

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Config files (may contain sensitive data)
.aiconfig/
.ai/

# Claude Code IDE files (should not be in repo)
.claude/

# Temporary files
tmp/
temp/

# Cache
.cache/
.parcel-cache/
.eslintcache
.astro/
.vercel/
.gradle/
.pnpm-store/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Local source comparison workspace
opensource-coding-anyone-can-copy/

# Private hosted SaaS workspace. Keep cloud billing/auth/server code out of the
# open-source repo until it is moved to its own private repository.
hosted/

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity


nvidia-nim
cc-cc

samples

# Local upstream Code-OSS source used only as an overlay target.
vscode-main/

# Read-only competitor reference trees. NEVER commit these.
# Licensing: third-party-main (xAI Grok Build) is Apache-2.0 -> port only with
# NOTICE + Apache 4(b) change notes. sample-code-main (T3 Code) has NO root
# license -> all rights reserved; reimplement behaviour only, never copy code.
sample-code-main
third-party-main
what.md


docs/